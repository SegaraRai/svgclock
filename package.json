{
  "name": "svgclock",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsx generate.mts && tsx bundle.mts",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "typecheck": "tsc --noEmit && deno check --unstable deno/server.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "@types/node": "^20.9.0",
    "esbuild": "^0.19.5",
    "tsx": "^4.1.1",
    "typescript": "^5.2.2",
    "wrangler": "^3.15.0"
  }
}
